export type TLoggedUser = {
  firstName: string;
  lastName?: string;
  email: string;
  role?: string;
  profileImage: string;
  phone?: string;
  whatsapp?: string;
  facebook?: string;
  isBlocked?: boolean;
};
